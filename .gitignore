# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Logs & temp
*.log
*.tmp
.env
.env.local

# Build tooling (downloaded locally, not committed)
tailwindcss.exe

# Secrets — NEVER commit (created on server via hPanel File Manager)
php/config.creds.php
